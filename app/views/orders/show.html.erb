<ul>
  <% @order.items.split("\n").each do |item| %>
    <li>
    <%= "#{format_price MenuItem.find_by_name(item).price} #{item}"%><br /> 
    </li>
  <% end %>
</ul>
<p><%= @order.instructions %></p>
<h1>
  <%="Total: #{format_price @order.price}" %><br />
  <%= "Pickup time: #{@order.pickup_time}" %>
save this info for paypal
buyer_1311953869_per@gmail.com
311971144
</h1>
  <% if !@order.completed || admin? %>
  <div class="actions">
    <%= link_to (image_tag "place-order-button.png"),complete_order_path(@order), :method => :put unless true%>
    <%= link_to (image_tag "place-order-button.png"), @order.paypal_url(order_url(@order), payment_notifications_url) %>
  </div>
  <delete>
    <%= link_to "Cancel Order", @order, { :confirm => "Are you sure?", :method => :delete } %>
  </delete>
  <% end %>
<br />
